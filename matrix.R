similar to a data frame but, 
the entire thing needs to be the same
type of data (mostly numeric)

one <- matrix(1:100, nrow=10)
one

two <- matrix(51:60, nrow=2)
two

three <- matrix(61:70, nrow=2)
three

dim(two)
dim(three)

two*three